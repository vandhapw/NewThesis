\section{Agentic AI Experiment (Without Quantum)}

The third experimental component evaluates the multi-agent pipeline without quantum-inspired heuristic enhancement. The pipeline processes 11 sensor events across 7 machines (M-1, M-2, M-3, M-4, M-32, M-40, M-47), orchestrated by the \texttt{glm-5.1:cloud} model via the Ollama Cloud API. The LLM benchmark (\S\ref{sec:llm_benchmark}) additionally evaluates \texttt{qwen3.5:397b-cloud} and \texttt{deepseek-v4-pro:cloud}.

In this configuration, all nine LLM agents (Monitoring, Diagnosis, Planning, Reporting, Correlation, Explainability, Self-Reflection, Scheduling, and Pattern Learning) base their decisions exclusively on classical risk scores computed by the \texttt{ReasoningEngine}, which applies static sensor thresholds and a composite risk formula derived from the training split (even machine IDs). \textbf{Post-audit safeguard:} No ground-truth fields (\texttt{status}, \texttt{failure\_type}) are passed to any LLM agent prompt; the LLM reasons only from sensor values and derived bounds. Table~\ref{tab:agentic_decisions_nonquantum} shows the per-machine decisions and risk assessments produced by the non-quantum agentic pipeline.

\begin{table}[htbp]
\centering
\caption{Per-Machine Decisions: Agentic AI Only (No Quantum)}
\label{tab:agentic_decisions_nonquantum}
\begin{tabular}{lccll}
\hline
\textbf{Machine} & \textbf{Events} & \textbf{Classical Risk} & \textbf{Action} & \textbf{Priority} \\
\hline
M-1  & 1 & 0.000 (LOW)    & monitor & auto-LOW-skip \\
M-2  & 1 & 0.000 (LOW)    & monitor & auto-LOW-skip \\
M-3  & 1 & 0.000 (LOW)    & monitor & auto-LOW-skip \\
M-4  & 1 & 0.000 (LOW)    & monitor & auto-LOW-skip \\
M-32 & 1 & 0.300 (MEDIUM) & monitor & P4 (bearing wear, 48h) \\
M-40 & 5 & 0.000 (LOW)    & monitor & auto-LOW-skip \\
M-47 & 1 & 0.000 (LOW)    & monitor & auto-LOW-skip \\
\hline
\end{tabular}
\end{table}

The non-quantum agentic pipeline correctly identifies Machine~32 as warranting attention (classical risk 0.30, MEDIUM, with vibration and humidity WARN flags), escalating it through the full nine-agent reasoning chain, which diagnoses bearing wear and recommends enhanced monitoring with a 48-hour surveillance window. However, the six machines with classical LOW risk (0.0) are all routed to passive monitoring via the \texttt{auto-LOW-skip} rule, and the pipeline generates only a single active decision out of 11 events (machine coverage 14.3\%). The feedback loop records only one feedback event, which is insufficient to trigger retraining (minimum threshold: 5 samples), limiting the learning loop's adaptability in this scenario.

\section{Agentic AI Experiment (With Quantum-Inspired Classical Heuristic Enhancement)}

The fourth experimental component evaluates the full QASAMAP pipeline with quantum-inspired classical heuristic enhancement. The same 11 events across 7 machines are processed and augmented by three heuristic layers: Pauli-Z multi-sensor encoding (implemented as \texttt{energy\_distance\_anomaly\_score}), QUBO-formulated priority ranking (implemented as \texttt{random\_constrained\_search}), and QAOA-formulated maintenance slot scheduling (implemented as \texttt{graph\_message\_passing\_score}). \textbf{Critical disclosure:} All three components are classical heuristics that mirror the mathematical structure of their quantum counterparts; they are \textbf{not} executed on quantum hardware. The mathematical formulation is detailed in Chapter~\ref{chap:quantum}.

The heuristic layer numerical results for the 7-machine demonstration are summarized in Chapter~\ref{chap:quantum} (Table~\ref{tab:heuristic_layer}). The per-sensor Pauli-Z expectations and composite $\langle Z \rangle_c$ are computed via the classical cosine-encoding formula (Equation~\ref{eq:composite_z}), not by executing the 5-qubit circuit shown in Figure~\ref{fig:pauliz_circuit} on a quantum processor.

The QUBO local search with minimum energy $-0.5144$ selects Machine~32 as the highest-priority machine. The greedy graph-message-passing scheduler assigns Machines 1, 2, 3, and 4 to Slot~A (IMMEDIATE/URGENT) and Machines 32, 40, and 47 to Slot~B (PLANNED/ROUTINE), with heuristic overrides escalating the decisions for Machines 1, 2, 3, 4, and 32 beyond what the classical risk layer would produce.

\section{Per-Machine Decision Comparison}

Table~\ref{tab:per_machine_comparison} presents the per-machine decision outcomes across all three pipeline configurations, directly demonstrating the value of the heuristic override mechanism. In the heuristic-augmented pipeline, six of the seven machines receive active maintenance actions, compared to one in the non-augmented agentic pipeline and zero in the traditional rule-based system.

\begin{table}[htbp]
\centering
\caption{Per-Machine Decision Comparison Across All Three Approaches}
\label{tab:per_machine_comparison}
\begin{tabular}{lcccl}
\hline
\textbf{Machine} & \textbf{Traditional} & \textbf{Agentic Only} & \textbf{Agentic + Heuristic} & \textbf{Override?} \\
\hline
M-1  & no\_action & monitor & inspect (P1)              & Yes (QUANTUM-WARN) \\
M-2  & no\_action & monitor & inspect (P2)              & Yes (QUANTUM-WARN) \\
M-3  & no\_action & monitor & inspect (P2)              & Yes (QUANTUM-WARN) \\
M-4  & no\_action & monitor & inspect (P2)              & Yes (QUANTUM-WARN) \\
M-32 & no\_action & monitor & inspect (P1)              & Yes (QUANTUM-WARN) \\
M-40 & no\_action & monitor & schedule\_maintenance (P3) & Yes (QUANTUM-WARN) \\
M-47 & no\_action & monitor & monitor                   & No \\
\hline
\end{tabular}
\end{table}

\section{Post-Audit Comparative Performance Evaluation}

The central result of the QASAMAP experimental evaluation is a rigorous post-audit comparative analysis conducted after the removal of ground-truth label leakage and the correction of quantum mislabeling. Two evaluation scopes are reported: a held-out test split (25 odd-ID machines, never used for bound derivation) and a full-dataset comparison (50 machines, pre-audit). All metrics are derived from the codebase outputs stored in \texttt{comparison\_results.json} and \texttt{full\_comparison\_results.json} (2026-05).

\subsection{Held-Out Test Split (25 Machines)}

Table~\ref{tab:post_audit_test} presents the post-audit detection performance on the held-out test split. The Rule-Based and AgenticAI approaches both achieve F1=0.00 because their conservative static thresholds fail to generalise to the subtle anomaly patterns in the test set. The Fleet-Relative detector (machine-centred median deviation) achieves F1=0.30, and the Sliding-Window detector (temporal context) achieves F1=0.48, establishing stronger baselines for future work.

\begin{table}[htbp]
\centering
\caption{Post-Audit Comparative Detection Performance (25 Held-Out Test Machines)}
\label{tab:post_audit_test}
\begin{tabular}{lcccccccc}
\hline
\textbf{Approach} & \textbf{TP} & \textbf{FP} & \textbf{FN} & \textbf{TN} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} & \textbf{Accuracy} \\
\hline
Rule-Based   & 0 & 4  & 10 & 11 & 0.00 & 0.00 & 0.00 & 0.44 \\
Agentic AI   & 0 & 4  & 10 & 11 & 0.00 & 0.00 & 0.00 & 0.44 \\
Fleet-Relative & 3 & 7  & 7  & 8  & 0.30 & 0.30 & 0.30 & 0.44 \\
Sliding-Window & 5 & 6  & 5  & 9  & 0.45 & 0.50 & 0.48 & 0.56 \\
\hline
\end{tabular}
\end{table}

\subsection{Full-Dataset Eight-Model Comparison}

Table~\ref{tab:full_comparison} reports the full 50-machine comparison including all neural baselines. The neural models (Transformer, LSTM, GNN, RL-QLearn, FedLearn, TransferL) achieve F1 scores of 0.73--0.76 but produce \textbf{zero true negatives} (TN=0 or TN=1), indicating systematic over-prediction in which nearly every machine is classified as anomalous. By contrast, the AgenticAI pipeline achieves perfect precision (1.00) but very low recall (0.20), yielding F1=0.33 and correctly identifying 20 true negatives while missing 24 failures.

\begin{table}[htbp]
\centering
\caption{Full Eight-Model Comparison (50 Machines, Pre-Audit / Full-Dataset Run)}
\label{tab:full_comparison}
\begin{tabular}{lcccccccc}
\hline
\textbf{Model} & \textbf{TP} & \textbf{FP} & \textbf{FN} & \textbf{TN} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} & \textbf{Accuracy} \\
\hline
RuleBased  & 0  & 0  & 30 & 20 & 0.00 & 0.00 & 0.00 & 0.40 \\
Transformer& 29 & 20 & 1  & 0  & 0.59 & 0.97 & 0.73 & 0.58 \\
LSTM       & 30 & 20 & 0  & 0  & 0.60 & 1.00 & 0.75 & 0.60 \\
GNN        & 30 & 20 & 0  & 0  & 0.60 & 1.00 & 0.75 & 0.60 \\
RL-QLearn  & 30 & 20 & 0  & 0  & 0.60 & 1.00 & 0.75 & 0.60 \\
FedLearn   & 30 & 20 & 0  & 0  & 0.60 & 1.00 & 0.75 & 0.60 \\
TransferL  & 30 & 19 & 0  & 1  & 0.61 & 1.00 & 0.76 & 0.62 \\
AgenticAI  & 6  & 0  & 24 & 20 & 1.00 & 0.20 & 0.33 & 0.52 \\
\hline
\end{tabular}
\end{table}

\subsection{Execution Time and Latency}

Table~\ref{tab:execution_time} quantifies the computational cost of each approach. Neural baselines complete prediction in milliseconds, whereas the AgenticAI pipeline requires 5,771.8~seconds ($\approx$1.6~hours) for a single prediction cycle due to nine sequential LLM API calls per machine event. This latency is a critical deployment consideration discussed in the following sections.

\begin{table}[htbp]
\centering
\caption{Execution Time Comparison (Seconds)}
\label{tab:execution_time}
\begin{tabular}{lccc}
\hline
\textbf{Model} & \textbf{Train (s)} & \textbf{Predict (s)} & \textbf{Total (s)} \\
\hline
RuleBased  & 0.000 & 0.000 & 0.000 \\
Transformer& 0.125 & 0.016 & 0.141 \\
LSTM       & 0.438 & 0.016 & 0.454 \\
GNN        & 0.250 & 0.016 & 0.266 \\
RL-QLearn  & 0.063 & 0.000 & 0.063 \\
FedLearn   & 0.030 & 0.000 & 0.030 \\
TransferL  & 0.058 & 0.000 & 0.058 \\
AgenticAI  & 0.000 & 5771.824 & 5771.824 \\
\hline
\end{tabular}
\end{table}

\section{LLM Benchmark}
\label{sec:llm_benchmark}

Table~\ref{tab:llm_benchmark} summarises the LLM benchmark (270 calls: 3 models $\times$ 6 agents $\times$ 5 machines $\times$ 3 runs). Qwen3.5 achieves the highest JSON parse rate (82.2\%), schema compliance (82.2\%), and reasoning quality (84.8\%), but at the highest latency (70.8~s average, 248.7~s p95). DeepSeek-V4 offers the fastest average latency (20.8~s) and best quality-per-dollar (23.2). The Reporting Agent consistently fails JSON parsing (0\% JSON rate across all models), emitting free-text instead of structured output and limiting downstream automation.

\begin{table}[htbp]
\centering
\caption{LLM Benchmark --- Overall Model Comparison}
\label{tab:llm_benchmark}
\begin{tabular}{lccc}
\hline
\textbf{Metric} & \textbf{GLM-5.1} & \textbf{Qwen3.5} & \textbf{DeepSeek-V4} \\
\hline
JSON parse rate (\%)   & 66.7 & 82.2 & 75.6 \\
Schema compliance (\%) & 66.7 & 82.2 & 75.6 \\
Reasoning quality (\%) & 71.7 & 84.8 & 73.1 \\
Latency avg (sec)      & 27.74 & 70.82 & 20.77 \\
Latency p95 (sec)      & 81.57 & 248.72 & 76.39 \\
Temporal consistency (\%) & 50.6 & 66.3 & 62.2 \\
Est.\ cost (benchmark) & \$0.0252 & \$0.0378 & \$0.0315 \\
Quality per dollar     & 28.4 & 22.4 & 23.2 \\
\hline
\end{tabular}
\end{table}

\section{System Capability Matrix}

Table~\ref{tab:capability_matrix} summarizes the feature and capability set available to each of the three evaluated approaches, illustrating the progressive enrichment of system capabilities from the traditional rule-based system to the full QASAMAP stack.

\begin{table}[htbp]
\centering
\caption{System Capability Matrix Across Three Approaches}
\label{tab:capability_matrix}
\begin{tabular}{lccc}
\hline
\textbf{Capability} & \textbf{Traditional} & \textbf{Agentic Only} & \textbf{Agentic + Heuristic} \\
\hline
LLM Agent Reasoning               & No  & Yes & Yes \\
Contextual Memory (Sliding Window)& No  & Yes & Yes \\
Learning Loop / Retraining        & No  & Yes & Yes \\
Multi-Sensor Cross-Reasoning      & No  & Yes & Yes \\
Kafka Data Ingestion (Ready)      & No  & Yes & Yes \\
QUBO-Formulated Priority Search   & No  & No  & Yes \\
QAOA-Formulated Graph Scheduling  & No  & No  & Yes \\
Pauli-Z Risk Encoding (Classical) & No  & No  & Yes \\
Heuristic Override (FN Guard)     & No  & No  & Yes \\
\hline
\end{tabular}
\end{table}

\section{Discussion}

\subsection{Agentic AI Pipeline Without Quantum Enhancement}

The non-quantum agentic pipeline processes 11 events across 7 machines and produces active maintenance decisions for only 1 out of 7 machines (Machine 32, classical risk = 0.300 MEDIUM), while the remaining 6 machines receive passive monitoring via the \texttt{auto-LOW-skip} rule. This 14.3\% machine coverage reflects a fundamental limitation of classical threshold-based risk assessment: machines operating below the MEDIUM risk boundary are invisible to the active decision-making pipeline regardless of whether their sensor patterns carry subtle multi-sensor risk combinations detectable by the heuristic layer.

The Monitoring Agent's assessment for Machine 32 --- bearing wear with vibration and humidity WARN flags at 48-hour urgency --- is diagnostically reasonable given the classical evidence (vibration = 67.09, WARN threshold = 55; humidity = 74.35, WARN threshold = 68). The Planning Agent's recommendation of enhanced monitoring at P4 rather than immediate inspection reflects the conservative classical risk assessment (risk = 0.300, boundary of MEDIUM level) and the high RUL (446 hours), which together suggest that urgent intervention is not yet warranted from a purely classical perspective. This conservative behavior is appropriate for a classical system but represents a potential gap when sub-threshold heuristic risk signals exist for other machines.

The limited feedback from the non-quantum run (1 feedback event vs.\ the minimum of 5 required for retraining) reflects the low active-decision rate of the pipeline. This is an important operational consideration: a classical-only agentic system that routes the majority of events to passive monitoring will accumulate insufficient feedback for learning loop adaptation, limiting the system's ability to improve through experience. The heuristic-augmented pipeline's higher active-decision rate (10 active decisions out of 11 events) generates richer feedback that supports more effective learning loop operation.

\subsection{Post-Audit Comparative Analysis}

The post-audit comparative analysis replaces the pre-audit KPI tables, which were found to contain fabricated metrics derived from label-leaking experimental procedures. The corrected evaluation reports two scopes: a held-out test split (25 machines) and a full-dataset comparison (50 machines).

On the held-out test split, both the Rule-Based and AgenticAI pipelines achieve F1=0.00, precision=0.00, and recall=0.00. This result is sobering: despite the rich capabilities of the nine-agent pipeline (root cause diagnosis, explainability, correlation, consensus, scheduling, pattern detection), its conservative static thresholds completely fail to detect anomalies in the held-out test set. The Fleet-Relative detector (F1=0.30) and Sliding-Window detector (F1=0.48) establish stronger baselines by exploiting machine-relative deviations and temporal context, respectively, suggesting that future agentic pipelines should integrate these detection strategies rather than relying solely on static thresholds.

On the full 50-machine dataset (pre-audit run, where ground-truth fields were previously leaked), the AgenticAI pipeline achieves perfect precision (1.00) but very low recall (0.20), yielding F1=0.33. This indicates that the pipeline is highly specific --- when it flags a machine, it is almost always correct --- but it misses 80\% of actual failures. By contrast, the neural baselines (Transformer, LSTM, GNN, RL-QLearn, FedLearn, TransferL) achieve F1 scores of 0.73--0.76 but produce \textbf{zero true negatives} (TN=0 or TN=1), revealing systematic over-prediction: they classify nearly every machine as anomalous. This over-prediction would be operationally catastrophic in a real factory, generating maintenance orders for the entire fleet. The AgenticAI pipeline's high specificity is therefore a genuine operational advantage, but its low sensitivity is a critical deployment blocker.

The latency comparison exposes a fundamental trade-off. The AgenticAI pipeline requires 5,771.8~seconds ($\approx$1.6~hours) per prediction cycle, versus milliseconds for neural baselines. This latency is driven by nine sequential LLM API calls per machine event. The LLM benchmark (Table~\ref{tab:llm_benchmark}) reveals that even the fastest model (DeepSeek-V4, 20.8~s average) cannot reduce the total cycle below $\sim$200~seconds per machine, and the Reporting Agent consistently fails structured output (0\% JSON rate), forcing manual parsing of free-text alerts.

\subsection{Implications for Industrial Deployment}

As discussed in Chapter~\ref{chap:tgcn}, Section~\ref{sec:deployment_implications}, the near-perfect performance of tree-based ensemble models on binary classification targets suggests that a hybrid decision architecture --- using fast ensemble inference for initial screening --- could substantially reduce the LLM agent invocation rate and improve system throughput.

The latency of 5,771.8~seconds per prediction cycle is a critical deployment blocker for real-time applications. Future work should investigate parallel agent invocation, caching of LLM responses, and lightweight local model deployment to reduce the per-event decision latency from hours to minutes.


\section{Summary}

This chapter presented the multi-agent system experiments and evaluation. The agentic pipeline without heuristic enhancement covers only 14.3\% of the machine pool with active maintenance decisions in the 7-machine demonstration, constrained by the classical risk engine's LOW-skip rule. The post-audit evaluation reveals F1=0.00 on the held-out 25-machine test split for both Rule-Based and AgenticAI pipelines, with the AgenticAI achieving F1=0.33 on the full 50-machine dataset (precision=1.00, recall=0.20). The LLM benchmark (270 calls across three models) exposes JSON parse rates of 66.7--82.2\%, reasoning quality of 71.7--84.8\%, and a 0\% structured-output rate for the Reporting Agent, alongside a prediction latency of 5,771.8~seconds versus milliseconds for neural baselines. These honest metrics replace the pre-audit fabricated KPIs and establish the current capabilities and limitations of LLM-based industrial agent systems.
